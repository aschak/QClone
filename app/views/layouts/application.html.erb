<!DOCTYPE html>
<html>
<head>
  <title>Seek</title>
  <link href='https://fonts.googleapis.com/css?family=Raleway:400,300,200,100,500,600,700' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>

  <script>
    window.seek_user = <%=
      if current_user
        {id: current_user.id, username: current_user.username, tags: current_user.tags}.to_json.html_safe
      else
        {id: 'blank', username: 'blank'}.to_json.html_safe
      end
    %>
  </script>

</head>
<body <%= "class=welcome" unless logged_in? %>>

  <a href="" class="logo">seek</a>

  <% if logged_in? %>
    <%= current_user.username %>
    <%= button_to "Sign Out", session_url, method: :delete, class: "btn btn-primary" %>
  <% else %>
    <%= link_to "Sign Up", new_user_url %>
    <%#= link_to "Sign In", new_session_url %>
  <% end %>


  <%= render "layouts/errors" %>

  <%= yield %>

</body>
</html>
